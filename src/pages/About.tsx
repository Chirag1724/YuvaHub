/**
 * Re-exports the About tab component so both
 * `src/pages/About.tsx` and `src/components/tabs/About.tsx`
 * resolve to the same implementation.
 */
export { default } from '../components/tabs/About';
